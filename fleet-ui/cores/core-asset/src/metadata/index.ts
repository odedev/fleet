import { metadata } from "@fleet/base";

import ResourceFileModel from './model/ResourceFileModel';

import LocationEnum from "./enumeration/LocationEnum";

metadata.setModel(ResourceFileModel);

metadata.setEnumeration(LocationEnum);
